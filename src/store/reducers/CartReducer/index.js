const initialState = {
    Basket: 0
}
const CartReducer = (state = initialState, action) => {
    switch (action.type) {
        case state:

            break;

        default:
            return state;
    }
}
export default CartReducer