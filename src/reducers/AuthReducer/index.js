const initialState = {
    user: null
}
const AuthReducer = (state = initialState, action) => {
    switch (action.type) {
        case state:
            break;
        default:
            return state;
    }
}
export default AuthReducer